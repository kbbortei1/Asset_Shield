# Gateway base URL. In dev, use your machine's LAN IP (NOT localhost) so a
# physical device / emulator can reach the backend. Only the gateway (:8080) is
# published; everything lives under /api/v1.
EXPO_PUBLIC_API_URL=http://192.168.1.50:8080/api/v1
